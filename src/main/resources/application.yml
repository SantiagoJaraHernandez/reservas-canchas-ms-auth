server:
  port: ${SERVER_PORT:8082}

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/authdb}

jwt:
  secret: ${JWT_SECRET:miClaveSecretaSuperSeguraParaDesarrollo2026}
  expiration: 86400000